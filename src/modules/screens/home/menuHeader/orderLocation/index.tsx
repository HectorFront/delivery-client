/** @name Dependencies */
import {memo} from "react";

type OrderLocationProps = {
    menuHeaderHeight: number | undefined
}

export const OrderLocation = memo(({ menuHeaderHeight }: OrderLocationProps): JSX.Element =>
    <div></div>
);